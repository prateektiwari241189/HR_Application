// $('#employees').append('<%= j render @employees %>');
<%# if @employees.next_page %>
  // $('.pagination').replaceWith('<%= j will_paginate @employees %>');
<%# else %>
  // $(window).off('scroll');
  // $('.pagination').remove();
<%# end %>
